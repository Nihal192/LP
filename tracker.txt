
Date : 07/27/24  
Name : Merge sorted array.
Method : 2 while loop , one checks if helper variable M and N are greater then zero then checking nums1[m-1]>nums2[n-1].
-  then we assign the nums1[last]=nums1[m-1]. "m-1" means the last element of helper variable M. decrement m by 1.
- else assign nums1[last]= nums2[n-1]. "n-1" means the last element of helper variable N. , decrement n by 1.
- last will also move forward in both cases so it will set to last-1
- 2nd While Loop for n>0 if yes then nums1[last]=nums2[n-1], n-=1,last-=1

Date: 07/21/24
Name: Remove Element
Mehtod:
 - Declare a varibale that is set to zero
 - now iterate through len(nums)
 - check if nums[i] does not equal to val's value. if it doesnt then go ahead and assign the nums[i] to the nums of the declared varibale that you done earlier to zero.
 - now decrement variable  by 1 as it will have to move through whole list
 - return the varibale in line of for loop not in line of if loop




Date :07/30/24  Name: Majority Element  Approach: sort the input , store lenght of input in some variable , return nums[n//2]. 
where nums is input and n is var that has legth of input



Date:08/06/2024
Name:Buy Sell Stock 1 (Easy)
Method: 
- first define L,R,MP . L=left pointer ,R = right pointer , mp = maximum profit
- check if right pointer is less then total length of prices , i.e (while r<len(prices))
- now check if prices[l]<prices[r] , if so then subrtact prices[r]-prices[l] and assign that to Profit.
- set maximum profit as maximum of (mp,profit)
- now in case if prices[l] is not less then price[r], then we assign the l pointer to r to move further . i.e l=r and then increment the r pointer
- at last under the for loop section return mp which is maximum profit



Date : 08/07/2024
Name : Jump Game
Mehtod:
 - set goal at the end of nums i.e len(nums)-1
 - now run loop in nums , set start ittreating from the End to Start.
 - check if i+nums[i]>= goal , if yes then assign goal=i .
 - return True if goal is 0 else False




 Date 08/11/2024
 Name : Ransom Problem

 Method: 
    - create two varibale (s1,s2)and asssign each of them  counter for both the strings ransomnote and magazine
    -now compare if s1&s2 == s1 if yes then return True else False


Date : 08/12/24
Name: Insert Delete GetRandom O(1)
Method :
   - you will be given 3 function skeleton 
   - intialize the self.data  to set() function as we will use it for add and Remove i.e self.data =set
   - inside add function look if we alreay have this element i.e if val in self.data
   - then if yes add it to set i.e self.data.remove(val) , return True
               -  else return false under if statement
   - same goes for deletion , check if self.data in val then self.data.remove(val) , return True
   - else return False under if condition

   - for random return use the simple method random. donot forget to return it in list 
   - i.e return random.choice(list(self.data))